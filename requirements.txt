# アプリ(ダッシュボード)の実行に必要な依存。Streamlit Cloud もこのファイルを読む。
#
# 完全固定(==)はやめ、範囲指定にしている。Streamlit Cloud が使う Python は予告なく
# 上がるため、C拡張を持つ numpy/pandas を固定すると新しい Python 用ホイールが無く
# インストール全体が失敗する(2026-08: Cloud が 3.14 になり pandas==2.2.3 が解決不能
# → plotly すら入らず ModuleNotFoundError になった)。
# 上限はメジャー単位で切り、破壊的変更(特に pandas 3.x)だけを除外する。
streamlit>=1.58,<2
plotly>=6.0,<7
pandas>=2.2,<3
numpy>=2.1,<3
requests>=2.32,<3
yfinance>=1.4,<2
pyarrow>=18
